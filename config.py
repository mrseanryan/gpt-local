IS_GPU_ENABLED=True # Requires NVidia graphics card with latest driver and version of CUDA to match ctransformers
LOCAL_GPU_LAYERS=8 # 8 worked for an NVidia card with 2 GB RAM, but maybe that also depends on the model?
